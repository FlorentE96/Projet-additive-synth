#include "modmatrix.hpp"

modMatrix::modMatrix()
{
    reset();
}

template<typename T1, typename T2>
void modMatrix::connect(T1 * _src, T2 * _dst) {
    links.push_back(Link(_src, _dst));
    links.src = src;
    links.dst = dst;
}

template<typename T1, typename T2>
void modMatrix::disconnect(T1 * _src, T2 * _dst) {
    
}

template<typename T1>
float modMatrix::process(T1 * _dst);
void modMatrix::reset();